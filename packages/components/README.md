# @platform-core/components

Reusable PlatformCore React components extracted from VLRC.

## Exports

- `Modal`
- `ConfirmModal`
- `ConfirmationDialog`
- `lazyLoad`

## Release

This package is managed by pnpm workspaces and Lerna fixed versioning.

```bash
pnpm version:packages
pnpm publish:packages
```
